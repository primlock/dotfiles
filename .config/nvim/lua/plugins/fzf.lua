-- fzf-lua is a plugin that provides a lua-based wrapper around the fzf command and allows users to leverage
-- the poiwer and speed of fzf directly within Neovim for tasks such as fuzzy finding files, searching
-- buffers, exploring git history, running commands and managing lists
return {
  "ibhagwan/fzf-lua",
  dependencies = { "nvim-tree/nvim-web-devicons" },
  opts = {}
}
